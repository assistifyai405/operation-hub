# Assistify OS — PRD

## Original Problem Statement
Build a premium AI business operating system called Assistify OS — an all-in-one dark-theme SaaS platform for entrepreneurs to manage clients, projects, AI agents, proposals, documents, tasks and operations. Pages: Login, Dashboard, Clients, Projects, Tasks, AI Chat, AI Agents, Proposals, Documents, Analytics, Settings. Design inspired by Linear, Notion, Stripe, Vercel — dark theme, electric violet accent, rounded cards, smooth animations, fully responsive.

## User Choices
- Real AI integration for AI Chat & AI Agents (Emergent LLM key, OpenAI gpt-5.4, streaming)
- Clean & geometric typography → Outfit font
- Electric violet accent (Linear-style)
- Production JWT auth (2026-06): dev-mode email links, soft email verification, seeded demo account owns existing data, all data scoped to organizationId

## Architecture
- Frontend: React 19 + React Router 7, Tailwind, shadcn/ui, Recharts, lucide-react. Fixed sidebar layout + glass header. AuthProvider context + ProtectedRoute; api.js token store (Bearer + refresh interceptor).
- Backend: FastAPI + MongoDB. Auth via PyJWT + bcrypt (auth.py). Access token (30m, Bearer + cookie) + rotating refresh token (httpOnly cookie /api/auth). All CRUD/AI/dashboard endpoints org-scoped via current_org dependency + require_project guard.
- LLM: emergentintegrations LlmChat, model openai/gpt-5.4, EMERGENT_LLM_KEY.

## User Personas
- Solo founders / agency owners managing multiple clients and projects who want AI leverage.

## Implemented (2026-06)
- AI Memory & Knowledge Brain — Sprint 7 (2026-07, COMPLETE): Assistify now continuously learns from the user and reuses that knowledge across the product. Backend `routers/memory.py` (prefix `/api/memory`): **Memory CRUD** on `db.memories` — `GET/POST /memories`, `PUT/DELETE /memories/{id}`, with fields `{organizationId, category, title, content, confidence, source (user|learned|seed), pinned, learning_enabled, keywords, created_at, updated_at}`; list supports category filter + sort (pinned-first stable sort). Categories: Business, Writing Style, Pricing, Customers, Projects, Processes, Frequently Used Terms, Products, Services, Brand Voice, Policies, Preferences, Relationships. **Controls**: `PATCH /memories/{id}/pin`, `PATCH /memories/{id}/learning` (toggle auto-learning), `POST /memories/merge` (combine near-duplicates). **Learning engine**: `POST /learn/event` logs user actions (edits/approvals/rejections) as pending events and auto-distills memories via LLM once pending>=4 (threshold), deduped by case-insensitive title, exceptions swallowed safely; `POST /learn/analyze` runs on-demand distillation. **Retrieval (RAG-lite)**: `GET /search?q=` keyword search over title/content/keywords; `POST /ask` answers questions grounded in stored memories (gpt-5.4); `GET /insights` surfaces synthesized business insights; `GET/POST /business-profile` generates & stores a structured business profile. **Smart-reuse / contextual injection**: the 4 document generators (`generate_plan/proposal/contract/invoice` in server.py) and the Floating Assistant now pull relevant memories (brand voice, pricing, preferences) into their prompts, so learned knowledge auto-applies to future generations. `GET /stats` returns totals + per-category counts. All org-scoped (verified isolation across 2 demo tenants). Demo seed pre-populates 6 starter memories per fresh tenant. Frontend `pages/KnowledgeBrain.jsx` (sidebar nav "Knowledge Brain"): brain stats grid, memory cards grouped by category with pin/edit-modal/save/delete (toast) + toggle-learning + new-memory + merge-mode (select + confirm), category chips, search box, sort, synthesized insight cards, and an "Ask your brain" box. `memoryApi` added to lib/api.js. Verified iteration_25 (backend 22/22 pytest `tests/test_knowledge_brain.py` — CRUD/filters/stats/merge/pin/toggle/learn-auto-distill/analyze/insights/business-profile/search/ask/org-isolation + smart-reuse regression on proposal generator & assistant chat; frontend 100% — full UI drive incl. create/search/pin/edit/toggle/ask/analyze/merge/delete/sort, no console errors). Re-verified live 2026-07-13: memories list/stats/insights/ask all 200 with real LLM answers. NOTE: Stripe/billing intentionally NOT touched.

- AI CRM & Sales Pipeline (2026-07): a full HubSpot/Pipedrive/Notion-style sales OS built on existing data (no duplicate models). **Clients enriched** with CRM fields (industry, company_size, website, address, notes, owner, next_follow_up, source, tags) — one source of truth. New `routers/crm.py`: **Leads/deals** (`db.leads`) with stages New→Qualified→Meeting Scheduled→Proposal Sent→Negotiating→Won/Lost, computed lead **score 0-100** (`_lead_score`), probability, expected close, **AI confidence**, color; full CRUD + `PATCH /crm/leads/{id}/stage` (drag-drop, appends stage_history) + `POST /crm/leads/{id}/convert` (creates & links a Project, idempotent). **AI Sales Assistant** `POST /crm/leads/{id}/ai-brief` (gpt-5.4, cached by context hash) returns relationship_summary, conversation_summary, missing_info, suggested_followup, objections, deal_health, win_probability, urgency, risk_level, next_best_action. **Contacts** (`/crm/contacts`, `/crm/contacts/{id}` with projects/leads/proposals/contracts/invoices/timeline, `/crm/contacts/{id}/ai-summary` stored relationship summary). Deterministic **follow-up recommendations** per stage/age; **customer timeline** merging leads/stage-changes/docs/activities/ai_activities. **Global CRM search** (`/crm/search`) across contacts/leads/projects/proposals/invoices. **Sales metrics** (`/crm/sales-metrics`): pipeline_value, weighted_pipeline, closing_this_week, leads_needing_attention, win_rate, avg_deal_size, conversion_rate, ai_insights, stage_breakdown. Demo seed adds 3 enriched clients + 6 leads across stages (~$132k pipeline). Frontend: `crmApi`; `pages/Pipeline.jsx` (native HTML5 drag-drop Kanban, search/sort/new-lead), `pages/CRM.jsx` (contact grid, search, tag filters), `pages/ContactDetail.jsx` (details, AI relationship summary, linked deals/projects/docs, timeline); `components/crm/LeadDrawer.jsx` (edit form, Save, Convert to project, **One-click AI** — Generate Proposal/Contract/Invoice, Improve Proposal, Write Follow-up, Prepare Meeting, Translate Email, Summarize Customer — all reuse existing generators + assistant priming via `?assist=`), `AISalesBrief.jsx`, `crmShared.jsx`; `components/DashboardSales.jsx` (6 sales KPIs + AI Sales Insights + Leads needing attention) on the Dashboard. Sidebar adds CRM + Pipeline. Verified iteration_24 (backend 12/12, frontend 100%: pipeline/CRUD/stage-move/convert/ai-brief/contacts/search/sales-metrics/org-isolation, drag-drop persistence, lead drawer, AI brief & relationship summary, no regressions/loops). NOTE: Stripe/billing/marketing/auth intentionally NOT touched per user.


- AI Opportunities & Recommendations (2026-07): Assistify now proactively scans the workspace and surfaces prioritized recommendations. Backend `routers/opportunities.py`: `_scan(org)` evaluates 11 rule types (invoice overdue/payment reminder, contract unsigned, proposal unsent, follow-up, project inactive, client not contacted, proposal improve, project no task, project no deadline, duplicate documents, client missing info), each producing {title, explanation, why, time_saved, confidence, score 0-100, priority Low/Medium/High/Critical, color green/yellow/orange/red, icon, one-click action{label,kind,link,command}}; auto-sorted by score. Endpoints: `GET /opportunities` (items+counts+total_time_saved), `GET /opportunities/brief` (Today's AI Brief: greeting + count lines + total time saved + top 3), `GET /opportunities/health` (Workspace Health 0-100 with 6 category sub-scores — Invoices/Contracts/Projects/Clients/Activity/Outstanding work — + reasons the score is lower), `POST /opportunities/dismiss` (7-day snooze in opportunity_dismissals), `POST /opportunities/archive-project/{id}` (real mutation → status 'Archived'). Frontend: new sidebar **Opportunities** page (`pages/Opportunities.jsx`) with priority filter chips + sorted cards, plus Dashboard widgets (`components/DashboardOpportunities.jsx` → DailyBrief + HealthWidget + top opportunities). Components: `opportunities/OpportunityCard.jsx` (animated score ring, priority chip, why-it-matters, action + snooze), `HealthWidget.jsx` (radial score + category bars + reasons), `DailyBrief.jsx`, `shared.jsx` (useOpportunityActions: deep-link nav + archive confirm + dismiss). One-click actions deep-link to the right Project Workspace tab or /clients; email/improve actions carry `?assist=<command>` which auto-opens the floating Assistant primed for that document (via AssistantContext.primed/primeAction, FloatingAssistant auto-run effect, ProjectWorkspace reads the param). Verified iteration_23 (backend 8/8, frontend 100%: list shape/sort/fields, brief, health 6 categories, dismiss persistence, archive + 404, org isolation, dashboard widgets, filters, one-click nav, assistant priming, snooze persistence; no regressions/loops). NOTE: Stripe/billing/marketing intentionally NOT added this sprint per user.


- AI Workspace Assistant — omnipresent AI employee (2026-07): a production-ready floating assistant available across the whole authenticated app (coexists with the /ai-chat Copilot; reuses its workspace snapshot + ai_service, no duplicated logic). Backend `routers/assistant.py`: (1) `GET /assistant/suggestions` returns context-aware action chips per scope/document type; (2) `POST /assistant/action` runs an ACTIONS registry (improve/rewrite/shorten/expand/fix_grammar/translate/exec_summary = TRANSFORM → returns apply payload; explain/summarize/pricing/timeline/deliverables/weak/risks/missing/tone/faq/email/cover/brainstorm = INFO → markdown answer) — TRANSFORM on a single section returns apply.mode='section' {target,value}; without a section it returns apply.mode='document' {values:{key:newvalue}} preserving list vs text types; every response carries an 'Explain Everything' report {what, why, impact, time_saved, confidence}; (3) `POST /assistant/chat/stream` real SSE streaming (gpt-5.4) that is context-aware (uses current page/document + copilot workspace snapshot); (4) `GET /assistant/history/{session}`. All org-scoped; messages persisted in `assistant_messages`. Frontend: `context/AssistantContext.jsx` (AssistantProvider mounted in Layout; `useAssistant`; `useAssistantDocument` hook that lets the 4 section editors — ProposalWriter/ContractWriter/InvoiceWriter/AIPlanner — register their live document so the assistant can read sections & apply changes; ProjectWorkspace sets project page-context). `components/assistant/FloatingAssistant.jsx`: animated FAB (framer-motion, positioned bottom-24 right-6 to clear the preview badge) + slide-in premium panel with context line ("I see you're on the Northwind proposal"), section-target dropdown, suggestion chips, slash commands (/rewrite /summarize /improve /email /translate /pricing /timeline /review /brainstorm), streaming markdown (`Markdown.jsx` via react-markdown+remark-gfm), report card, and Apply-to-document / Undo / Copy / Regenerate. Empty-doc guard shows a friendly "add content first" hint instead of a no-op apply. `assistantApi` added to lib/api.js. Verified iteration_22 (backend 9/9, frontend 20/20 incl. apply→undo lifecycle, slash commands, SSE streaming, and a no-infinite-render-loop regression across tab switches — an earlier loop caused by depending on the whole context object was fixed by depending only on stable callbacks). Added deps: react-markdown, remark-gfm.


- AI Workspace (2026-07): completed the user-requested central hub (merged the old AI History page into it — `/ai-history` now redirects to `/ai-workspace`; new sidebar nav item "AI Workspace" + user-menu item). Backend: the 4 generators (`generate_plan/proposal/contract/invoice` in server.py) now capture generation duration and pass `gen_ms`, `client_name`, `project_name` to `aia.log_ai_activity` (schema extended in `ai_activity._entry`). New endpoints in `routers/ai.py`: `GET /ai/workspace/stats` (total_actions, total_time_saved, week/month counts, avg_confidence, avg_gen_ms, by_type breakdown, busiest_day, 7-day sparkline), `GET /ai/workspace/search?q=&type=&range=&sort=` (regex text search over title/explanation/client/project, type + time-range filters, sort newest/oldest/most_saved; returns items + type list), `GET /ai/workspace/versions` (documents with >1 version — proposals/contracts/invoices via history array, plans via multiple docs — each with version timeline + deep link to the workspace compare tab). Demo seed (`_seed_demo_data`) now pre-populates 6 sample ai_activities (spread over days, with gen_ms/client/project) + a versioned proposal (v1+v2) and plan (v1+v2) and sets `ai_backfilled=True`, so the workspace shows value on first visit. Frontend `pages/AIWorkspace.jsx`: premium dark/violet page with 8 sections — header + lifetime time saved, 5-card stats grid + sparkline, quick actions, smart search + 3 filter dropdowns, day-grouped timeline (Today/Yesterday/This Week/Earlier, click routes to project), proactive insights, and version-compare cards that open a dialog (with DialogDescription for a11y) listing versions + "Open side-by-side compare". `aiApi.workspaceStats/workspaceSearch/workspaceVersions` added. Verified iteration_21 (backend 9/9 new `tests/test_ai_workspace.py` incl. org isolation + generate-metadata regression; frontend 100% — all 8 sections, search/filter/sort, version dialog, redirect, nav). No UI redesign; no existing functionality removed.


- AI Action Report (2026-07): a premium results card that appears immediately after every successful AI generation (proposal, contract, invoice, project plan; component is generic for future tools). Built on the existing dark/violet language, animates in (fade-up + staggered checklist). Backend `ai_activity.build_ai_report(type, project, content)` returns a deterministic, LLM-free report — confidence (88–99, derived from data completeness) + human note, per-type plain-language "what Assistify did" checklist, 3–6 contextual "why" explanations, time saved (proposal 23/contract 12/invoice 6/plan 15), and 4 quality indicators — added to all 4 generate endpoint responses as `report`. Frontend `components/ai/AIActionReport.jsx` renders: (1) success banner (doc type, generation time, timestamp), (2) checklist, (3) time saved + lifetime (via aiApi.timeSaved, with loading skeleton), (4) AI Confidence ring card, (5) Why decisions, (6) suggested next-step buttons (Review/Edit/Download PDF/Export Word/Send to Client — only those with handlers render; Plan shows Review+Edit only), (7) green-check quality indicators. Wired into ProposalWriter/ContractWriter/InvoiceWriter/AIPlanner: on successful generate, the report shows first; Review/Edit reveal the existing editor (content intact), Download/Export/Send reuse existing handlers. Responsive, no placeholders/lorem. Verified iteration_20 (backend 7/7 new `test_ai_action_report.py`, frontend 100% across all 4 generators + regression; fixed a minor mid-animation overflow by switching checklist rows to fade-up). No existing functionality removed.

- Sprint 2 "Make Assistify Feel Alive" (2026-07): transformed the product into an "AI employee" experience — every AI action is now visible, explained, and quantified (no redesign / no nav / no new modules). (1) **Global AI Activity engine** (`ai_activity.py`): every AI action (proposal/contract/invoice/plan generation) logs an entry with icon, human label, title, plain-English explanation, source page, timestamp, and estimated time saved; one-time `ensure_backfill` seeds activities from existing docs. (2) **Live AI Workflow** (`components/ai/AIWorkflow.jsx`): replaced bare spinners in all 4 generators with premium staged progress ("Reading client info ✓ → Understanding project ✓ → Writing proposal ▓▓▓") that completes when the real request returns. (3) **Proactive insights + "Why?"** (`routers/ai.py` /insights): rule-based detection (overdue invoices, stale sent proposals, tasks due today, stalled projects) with a cached-LLM copy polish (6h TTL, safe rule-copy fallback), each explaining WHY in human terms. (4) **Time saved** (/time-saved): today/week/month/lifetime aggregates (proposal 23m, contract 12m, invoice 6m, plan 15m, etc.). (5) **Dashboard AI section** (`DashboardAIAssistant.jsx`): greeting + "what Assistify completed today" + time-saved-today + proactive insight cards, above the unchanged KPIs/charts. (6) **Intelligent notifications** (/notifications): bell now shows AI-phrased insight + completion items (icon/title/explanation), not generic text. (7) **AI History page** (`/ai-history`, reachable via dashboard "View all" + user-menu item): grouped Today/Yesterday/This Week/Earlier with what/why/time-saved + lifetime total. (8) **Microinteractions**: success pop, animated progress, fade-up, keyboard focus. Added `ai_activities` compound index (organizationId, created_at) for scale. Verified iteration_19 (backend 13/13 new `test_sprint2_ai.py` + all prior suites green; frontend 100% — dashboard section, live workflow, history, notifications, insights all confirmed; no regressions).

- P1 Launch-Readiness (2026-07): shipped all 5 P1 audit items (no new features / no redesign / no workflow changes). (1) **Accessibility** — aria-labels on all icon-only header/nav buttons (mobile-menu, mobile-close, command-palette-mobile, notifications-btn, user-menu), alt text on images/avatars/logos (Layout AvatarImage, AIChat agent avatars, settings logo preview, BrandedDocPreview), global `:focus-visible` outline for keyboard nav, notification dot only renders when notifications exist. (2) **Security** — removed the JWT `?auth=<token>` query param from `documentsApi.fileUrl` and `settingsApi.imageUrl`; `download_document` & `serve_branding_image` now authenticate ONLY via Bearer header or the httpOnly `access_token` cookie (verified: `?auth=` ignored → 401; cookie accepted → 404 for unknown asset). No tokens in URLs/history/logs. (3) **Production copy** — replaced the dev-mode "check backend logs" verification toast with "Verification email sent — please check your inbox." (4) **Email verification** — kept soft verification; added a dismissible `verify-email-banner` (Resend + dismiss, dismissal persisted in sessionStorage) for unverified users; added `_guard_send(user,status)` blocking save of proposal/contract/invoice with status "Sent" for unverified users (403), applied to save_proposal/save_contract/save_invoice (guard safely skips Copilot's internal calls via isinstance check). (5) **Copilot optimization** — `_copilot_snapshot` now sends a bounded slice (top 20 clients by value, 25 recent projects, 10 soonest-overdue tasks via query, 30 recent invoices, 8 activities) with accurate totals via count_documents; identical structure, lower token usage. Also moved the Sonner toaster to bottom-right so it no longer overlaps the banner dismiss button. Verified iteration_18 (backend 17/17 new `tests/test_p1_launch.py`, frontend banner/aria/images confirmed, verified-user regression intact) + prior suites still green (copilot_branding 31, email 13, demo_billing 10).

- P0 Launch-Audit fixes (2026-07): (1) **Removed hardcoded demo credentials** — login fields no longer pre-fill jordan@assistify.io/Assistify2026! (empty on load); password no longer ships in the client bundle. Added an **"Explore Demo" flow**: new public `POST /api/auth/demo` provisions a brand-new ISOLATED demo tenant on each click (own organization + anonymous `demo+<rand>@assistify.demo` user with a random unusable password, `isDemo:true`), seeds a small sample workspace (3 clients, 2 projects, 3 tasks) scoped to that org, and issues the standard JWT session (rotating refresh cookie + access token). Demo tenants never share data (verified: two demos get distinct organizationId; a client created by demo1 is invisible to demo2). Frontend: `authApi.demo` + `AuthContext.demo` + `explore-demo-btn` on Login. (2) **Monetization readiness without Stripe** — added `BILLING_ENABLED` flag (`REACT_APP_BILLING_ENABLED`, default false). While disabled: sidebar "Upgrade to Pro" card renders as a no-button "Pro plan — coming soon" note (dead `upgrade-btn` removed); Settings→Billing shows a clean `billing-coming-soon` placeholder (no upgrade button, no fake active plan/price; informational usage kept). Verified iteration_17 (backend 10/10 new `tests/test_demo_and_billing.py`, frontend 100%, no bugs) + regression email 13/13 & copilot_branding 31/31 still pass. NOTE: register/demo/forgot share a 10/hr/IP in-memory rate limit (restart resets); dead file `data/mock.js` still holds a sample email (never imported) — safe, recommend deleting in a cleanup pass.

- Real Email Delivery via Resend (2026-07): new `email_service.py` sends branded HTML transactional emails (email verification + password reset) through the Resend API, run non-blocking via `asyncio.to_thread`. Config from env: `RESEND_API_KEY` (optional) + `FROM_EMAIL` (default onboarding@resend.dev; set to onboarding@assistify.dev). **Dev-mode fallback**: when `RESEND_API_KEY` is empty/missing, no email is sent and the verify/reset link is logged + returned in the API response (preserves the testable dev flow). **Production**: when the key is set, real emails are sent and links are NEVER exposed in API responses. Send success/failure is logged safely and never raises — a provider error still lets register/forgot succeed. Branded template (`_shell`) uses org name + primary color from settings; internal `/api/settings/image/` logos are intentionally not embedded (email clients can't pass auth). Endpoints touched: register, forgot-password, resend-verification (link exposure gated on `email_service.is_enabled()`); `_email_brand(org_id)` helper added. Zero frontend changes. To enable real delivery later: set `RESEND_API_KEY` in backend/.env and restart — no code changes. Tests: `tests/test_email_delivery.py` — 13 passing (unit: disabled/enabled detection, dev fallback skips Resend, verification+reset sent with correct recipient/branded subject/embedded link/from, send-failure safe, internal-logo not embedded; HTTP: register/forgot expose links only in dev, generic response for unknown email, verify+reset+login+me flows intact). Also verified production mode manually (key set → no link exposed, failure logged, auth uninterrupted). Regression: copilot_branding 31/31 still pass. NOTE: register endpoint has a 10/hr/IP in-memory rate limit — running multiple heavy HTTP test suites back-to-back can exhaust it (restart backend to reset); not a product bug.

- Backend refactor — focused router extraction (2026-07): split the `server.py` monolith (2306 lines) into modules with **zero behavior change**. New `core.py` (Mongo client/db, `now_iso`, `log_activity`, `ai_service`/EMERGENT_LLM_KEY), `dependencies.py` (`public_user`, `current_user`, `current_org`, `require_project`, `rate_limit`, `_client_ip`), `routers/copilot.py` (message/execute/history/suggestions + all copilot helpers/models; lazily imports generators/models from server to avoid circular import), and `routers/exports.py` (6 proposal/contract/invoice PDF/DOCX endpoints + `_brand_context`/`_safe_filename`/doc-fetch helpers; lazily imports `_merged_settings`). server.py now imports these and `app.include_router`s the two new routers (same `/api` prefix → identical paths). server.py down to 1857 lines. CRUD/generators/dashboard/settings/auth remain in server.py by design. Verified: `python -c import server` clean, all endpoint groups 200, full suite **31/31 passing**, no regressions. No frontend changes.


- Copilot Execute Security Hardening (2026-07): `/api/copilot/execute` no longer trusts any client-supplied action payload. When `/copilot/message` proposes an action, the server persists it in a new `copilot_pending_actions` collection (id/action_id, session_id, organizationId, tool, params, preview, consumed flag, 30-min expiry) and returns only a confirmation token (`action_id`). Execute now requires `action_id`, looks up the pending action scoped to (org + session), rejects missing token (400), forged/unknown token (404), already-consumed token (409, atomic single-use claim via update_one), and expired token (410). Params are read from the server-stored record — a caller can no longer forge tool/params or bypass the confirmation UX. Cross-org isolation enforced (another org's token → 404 and does not consume the original). Every executed Copilot action now logs a `copilot_action` activity (create_client/create_project/generate_*). Removed dead `if False else None` branch in create_project. Frontend api.js + AICopilot.jsx updated to send `action.action_id`. Tests: added TestCopilotExecuteSecurity (5 cases: missing-token, forged-token, single-use/double-execute, cross-org, activity-logging) and updated existing create/execute tests to the token flow — full suite 31/31 passing (26 prior + 5 new). Backlog for later: sign/encrypt token, in-process brand-logo TTL cache, LLM snapshot truncation/RAG, frontend send debounce.
- Assistify Copilot 2.0 — Unified AI Command Center (2026-06): new /ai-chat page (copilot-page) with a single natural-language command bar backed by GET/POST /api/copilot/{message,execute,history,suggestions}. Reads real org-scoped workspace snapshot (clients/projects/tasks/invoices/activity) and answers questions with real data; detects create/generate intents and renders a confirmation action card (Confirm/Cancel) before any write. Supported tools: create_client, create_project, generate_plan/proposal/contract/invoice (generators resolve the target project by name, org-scoped). Session memory persisted per session_id and restored on reload. Real-data suggestion chips (overdue invoices/tasks, behind-schedule projects, top clients). Legacy AI chat preserved at /ai-chat-classic. Verified iteration_16 (backend 26/26, frontend 100%: read queries, action confirm+execute, suggestions, session memory).
- Professional Client Documents — Branding (2026-06): proposal/contract/invoice PDF & DOCX exporters (*_export.py) now inject org branding (logo, primary/secondary colors, business details, per-doc footers) from organization settings. Live BrandedDocPreview.jsx on each Project Workspace generator tab (branded-preview-proposal/contract/invoice + branded-preview-footer/-logo/-toggle). All 6 export endpoints require auth and are org-isolated. Verified iteration_16 (all 6 exports return valid PDF/DOCX, cross-org 403/404).
- Settings & Workspace Management: full production Settings area with 10 sections. General (user profile: name/avatar/language/timezone → PATCH /api/auth/profile). Organization (name, logo, website, business email, phone, address, VAT, KVK, default currency & VAT). Branding (primary/secondary colors, brand + PDF logo uploads to object storage, proposal/contract/invoice footers). AI Settings (default provider, proposal/contract tone, temperature slider, invoice notes — future-ready). Documents (proposal/contract/invoice prefixes wired into invoice numbering, numbering start, PDF page size & accent). Notifications (7 per-user email/product/security/billing/task/digest/AI toggles on the user record). Security (change password, active sessions, recent logins, log-out-all-devices → POST /api/auth/logout-all). API Keys, Integrations = future-ready placeholders. Billing = placeholder showing Pro plan, seats, and REAL usage counts + Upgrade CTA (Stripe-ready). Backend: org settings persist on the organization record (whitelisted keys per section), notif prefs on the user; GET /api/settings + PATCH /api/settings/{organization,branding,ai,documents,notifications}, POST /api/settings/upload-image (PNG/JPG/GIF/WEBP only), GET /api/settings/image/{id}, GET /api/settings/billing|api-keys|recent-logins. Verified iteration_15 (backend 18/18, frontend 100% across all 10 tabs, persistence + org isolation confirmed, zero bugs, no regressions).
- Complete Document & Data Integration: removed ALL mock/placeholder data — every page/widget now reads real org-scoped backend data. Real file uploads via Emergent object storage (storage.py): POST /api/documents/upload (25MB cap + extension allow-list), PUT rename, GET /{id}/file download (auth via Bearer/cookie/?auth=), plus server-side paginated library endpoints GET /api/library/{documents,proposals,contracts,invoices} (search/status filter/sort, page_size cap 50). New standalone Contracts (/contracts) and Invoices (/invoices, with financial totals + status tabs) pages; Documents page fully wired (upload/view/rename/delete/search/filter/pagination); Proposals page lists all org ai_proposals. Analytics (/analytics) rewritten to GET /api/analytics (real revenue-over-time, projects-by-status, doc output, task completion). Layout notifications now from GET /api/notifications (real activities). Global search (/api/dashboard/search) extended with documents group + deep-links to workspace tabs (?tab=proposal/contract/invoice). Sidebar gained Contracts & Invoices. Verified iteration_14 (backend 25/25, frontend 100%, org isolation confirmed, zero bugs).
- First-Run Experience & Onboarding: 4-step onboarding wizard (Welcome → Create first Client [company/name/phone/email] → Create first Project [name/description/status] → Guide to AI generators) that auto-launches on a new user's first login and can be skipped. Backend persists `onboardingCompleted` on the user record (register=false, demo/seeded & backfilled existing users=true so they never see the wizard). New endpoints GET /api/onboarding (auto-derives a 6-item checklist — client/project/plan/proposal/contract/invoice — from real org data + completion %) and POST /api/onboarding/complete. Dashboard shows a dismissible "Getting started" checklist card with live % that hides at 100%. Added optional `phone` to ClientCreate. Professional empty states + contextual HelpTip (?) tooltips added to Documents & Proposals pages (Clients/Projects/Tasks already had them). Verified iteration_13 (backend 6/6, frontend 100%, existing users & all features unaffected — no regressions).
- Production Authentication: full JWT auth replacing static login. Backend (server.py + auth.py): register (auto-creates Organization), login (bcrypt, brute-force lockout 5 attempts/15min), logout (session revoke), rotating refresh tokens (httpOnly cookie), /auth/me, forgot/reset password (dev-mode links returned in response), email verification (soft, dev-mode link), profile update (name/avatar/timezone/language/company), change-password, sessions list/revoke, in-memory rate limiting on register/forgot. New collections: users (id, firstName, lastName, email, passwordHash, emailVerified, avatar, role, organizationId, timezone, language, createdAt, updatedAt, lastLogin), organizations, sessions, password_reset_tokens (TTL), email_verification_tokens (TTL), login_attempts. Multi-tenancy: ALL existing collections gained organizationId; every CRUD/AI/dashboard query scoped by org; startup seeds demo (jordan@assistify.io / Assistify2026!) and backfills legacy data to its org. Frontend: AuthContext + ProtectedRoute, real Login/Register/ForgotPassword/ResetPassword/VerifyEmail pages, api.js Bearer+refresh interceptor, Layout uses real user + logout + unverified badge, Settings Profile & Security (change-password) tabs functional. Verified iteration_12 (backend 26/26, frontend 100%, zero regressions on Dashboard/CRUD/Workspace/AI generators).
- Executive Dashboard (real MongoDB-aggregated data) — replaced all placeholder dashboard data. New backend endpoints: GET /api/dashboard/summary (single aggregation returning kpis, financial, project_status, recent_activity, upcoming_tasks, recent_clients — no N+1 queries, no _id leaks) and GET /api/dashboard/search (global search across clients/projects/invoices/contracts/proposals). Frontend Dashboard.jsx renders 10 live KPI cards (clickable → navigate), Quick Actions (Create Client/Project, Generate AI Plan/Proposal/Contract/Invoice), Financial Summary (Revenue/Outstanding/Avg Invoice + bar chart by invoice status), Project Status progress bars, Recent Activity, Upcoming Tasks, Recent Clients, and a debounced Global Search with grouped results dropdown. Verified iteration_11 (backend 14/14 new dashboard tests, frontend 100% incl. regression on Project Workspace — no existing features broken).
- AI Invoice Generator ("Invoice" tab in every Project Workspace): auto-uses client, project, proposal, contract, timeline & payment terms to generate a professional invoice via the shared AI service layer (prompts in invoice_config.py). Auto-generated invoice number (INV-YYYYMM-####), editable line-items table with instant client-side recompute (line total, subtotal, configurable VAT %, grand total); backend recomputes on save as source of truth. Header (number, editable name, status, issue/due dates) + Generate/Save/Regenerate/Version History/Compare/Export PDF/Export DOCX. New ai_invoices collection (invoiceNumber, title, projectId, clientId, proposalId, contractId, status, content, lineItems, subtotal, vat, total, version, history, timestamps) with non-destructive restore + compare. Dedicated invoice PDF/DOCX exporters (invoice_export.py). Statuses Draft/Generated/Sent/Paid/Overdue/Cancelled/Archived. Logs invoice_generated/saved/exported/restored. Cascade-deletes with project. Verified iteration_10 (15/15 new backend, 32/32 regression, frontend 100%; Proposal/Contract/Planner untouched).
- AI Contract Generator ("Contract" tab in every Project Workspace): auto-uses client info, project info, AI Proposal, AI Planner, timeline, tasks, notes, documents to generate a 17-section legally-structured service agreement (GDPR-ready) via the shared provider-agnostic AI service layer; prompts in contract_config.py. Header (editable name, status, version, last generated) + Generate/Save/Regenerate/Version History/Compare/Export PDF/Export DOCX. New ai_contracts collection (title, projectId, clientId, proposalId, status, content, version, history, timestamps) with non-destructive restore + compare. Server-side PDF (reportlab) & DOCX (python-docx) via generalized exporters (backward-compatible with Proposal). Statuses Draft/Generated/Sent/Signed/Expired/Cancelled/Archived. Logs contract_generated/saved/exported/restored. Cascade-deletes with project. Verified iteration_9 (backend 16/16, frontend 100%, Proposal/Planner/existing features untouched).
- AI Proposal Writer ("Proposal" tab in every Project Workspace): auto-uses full project context (client, project, AI Planner, notes, documents, tasks, timeline) to generate 15 editable client-facing sections via a provider-agnostic AI service layer (ai_service.py) with prompts in config (proposal_config.py). Header (editable name, status, version, last generated) + Generate/Save/Regenerate/Version History/Export PDF/Export DOCX. New ai_proposals MongoDB collection (title, projectId, clientId, status, content, version, history, timestamps) with restore + side-by-side compare. Real server-side PDF (reportlab) & DOCX (python-docx) exports. Statuses Draft/Generated/Sent/Accepted/Rejected/Archived. Logs proposal_generated/saved/exported. Cascade-deletes with project. Verified iteration_8 (backend 16/16, frontend 100%, existing features untouched).
- AI Project Planner ("AI Planner" tab in every Project Workspace): analyzes full project context (client, description, notes, tasks, documents, timeline, status) via gpt-5.4 and generates 9 structured sections (Executive Summary, Business Goal, Technical Requirements, Recommended Plan, Milestones, Suggested Tasks, Estimated Timeline, Risks, Next Actions). Generate/Regenerate, edit-before-save, version history with timestamps saved to MongoDB (plans collection), side-by-side version compare, and Export to PDF (print window). Plans cascade-delete with project; saving logs a plan_generated activity. Verified iteration_6/7 (backend 6/6, frontend 100% after activity-refresh fix).
- Project Workspace at /projects/:id with 7 tabs (Overview, AI Chat, Tasks, Documents, Proposals, Notes, Activity Timeline). Per-project AI chat with persisted, isolated history (session_id=project-<id>). Auto-logged activity timeline (project/task created, task completed, proposal generated, document uploaded). New collections: documents, proposals, activities; projects gained description+notes; project delete cascades docs/proposals/activities. Verified iteration_5 (31/31 backend + all frontend, incl. chat isolation).
- Dashboard wired to live CRUD data: real counts (Total Clients, Active Projects, Open Tasks, Completed Tasks), latest 5 clients, next 5 open tasks (sorted by due), live Projects-by-Status pie, loading + empty states, clickable stat cards. Revenue & AI-activity charts remain placeholder. Verified iteration_4 (100%).
- CRUD persistence (MongoDB) for Clients, Projects, Tasks with modals, validation, empty states, delete confirms. Projects link to Clients; Tasks link to Projects. Deleting a parent unlinks children. Backend enriches list responses with client_name/project_name and client project counts. Endpoints: /api/{clients,projects,tasks} GET/POST/PUT/DELETE. Verified 24/24 backend + all frontend flows (iteration_3).
- Command Palette (Ctrl/Cmd+K) with navigation + AI quick actions (prompt prefilled into AI Chat). Verified iteration_2.
- All 11 pages with premium dark UI, electric violet theme, Outfit font, animations.
- Dashboard: revenue/stat cards, revenue area chart, projects pie, recent clients, upcoming tasks, AI agent activity bar chart, quick actions, notifications dropdown.
- Clients (table), Projects (kanban), Tasks (interactive checklist + filters), Proposals (list), Documents (grid), Analytics (4 charts), Settings (tabbed).
- AI Agents page (4 personas from backend) + AI Chat with streaming SSE and agent switcher.
- Placeholder data in frontend/src/data/mock.js.
- Verified: backend 100%, frontend 100% (testing agent iteration_1).

## Known Status
- AI Chat/Agents return live responses ONLY once the Emergent LLM key has balance. Currently $0 → returns budget error (user opted to fund later). UI flow works end-to-end.

## Backlog
- P1: Persist assistant error markers / show error toast on AI failure.
- P1: Add data-testid to kanban columns/cards.
- P2: CRUD persistence for clients/projects/tasks (currently mock).
- P2: Real auth + multi-user.
- P2: Health endpoint to verify LLM key validity.

## Next Tasks
- Fund LLM key and validate live AI chat.
- Wire real data persistence for core entities if requested.
